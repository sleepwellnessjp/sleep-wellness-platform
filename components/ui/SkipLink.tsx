/** Skip to main content — first focusable element for keyboard / screen readers. */
export default function SkipLink() {
  return (
    <a href="#main-content" className="sw-skip-link">
      メインコンテンツへスキップ
    </a>
  );
}
